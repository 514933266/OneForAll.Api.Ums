# OneForAll.Api.Pruner 项目开发规范

> 适用于本服务（数据清理与同步）的日常开发与维护，所有新增代码应遵循以下约定。

## 一、项目分层与依赖方向

| 层 | 目录 | 职责 | 备注 |
|---|---|---|---|
| Host | `Pruner.Host` | 控制器、过滤器、Quartz 任务、Startup、AutoMapper Profile | 唯一入口，可引用所有层 |
| Application | `Pruner.Application` | 应用服务（当前为空，按需扩展） | 命名以 `Service` 结尾 |
| Domain | `Pruner.Domain` | 实体、Form、仓储接口、Manager | 不引用 Host / Repository |
| Repository | `Pruner.Repository` | 仓储实现 | 命名以 `Repository` 结尾 |
| HttpSevice | `Pruner.HttpSevice` | 对外 HTTP 服务 | 命名以 `Service` 结尾 |
| Public | `Pruner.Public` | 公共模型（AuthConfig、枚举等） | 各层可引用 |

- 依赖方向：Host → Application → Domain ← Repository，禁止反向引用
- 仓储接口定义在 Domain 层（`Pruner.Domain/Repositorys`），实现类必须实现对应接口
- Autofac 按后缀约定自动注册：`*Service`、`*Manager`、`*Repository` + `AsImplementedInterfaces()`，新类型命名后缀不可省略

## 二、API 接口设计规范

### 1. 查询接口：直接返回结果对象（禁止包 BaseMessage）

```csharp
// 分页查询
[HttpGet]
public async Task<PageList<DsDeleteConfig>> GetPageAsync(int pageIndex = 1, int pageSize = 10, string key = "")
{
    if (pageIndex < 1) pageIndex = 1;
    if (pageSize < 1) pageSize = 10;
    return await _repository.GetPageListAsync(pageIndex, pageSize, key);
}

// 详情查询（不存在返回 null，前端判空）
[HttpGet("{id}")]
public async Task<DsDeleteConfig> GetAsync(int id)
{
    return await _repository.FindAsync(id);
}

// 列表查询
[HttpGet]
public async Task<List<DsGlobalConfig>> GetListAsync()
{
    return (await _repository.GetListAsync()).ToList();
}
```

- 分页结果统一使用框架对象 `OneForAll.Core.PageList<T>`（字段：`Total`/`Items`/`PageIndex`/`PageSize`/`TotalPage`），禁止自建分页类
- 查询异常统一由 `ExceptionFilter` 返回 `{ Status: false }`，Controller 内不捕获

### 2. 写操作：接收 Form 实体 + AutoMapper 映射，返回 BaseMessage

```csharp
[HttpPost]
public async Task<BaseMessage> AddAsync([FromBody] DsDeleteConfigForm form)
{
    var msg = new BaseMessage();
    if (form == null || string.IsNullOrEmpty(form.TableName))
        return msg.Fail(BaseErrType.InvalidParameter, "表名不能为空");

    var entity = _mapper.Map<DsDeleteConfig>(form);
    entity.Id = 0;
    var count = await _repository.AddAsync(entity);
    if (count > 0)
        return BaseMessage.Success("新增成功", entity);
    return msg.Fail("新增失败");
}
```

- **所有需要前端传参的写操作（Add/Update）必须在 Domain 层定义 Form 实体**（`Pruner.Domain/Models`），禁止直接接收实体、禁止手动逐属性赋值
- Form 只包含可编辑业务字段，运行状态字段（如 `LastDelId`/`LastDelTime`）不进 Form
- Form 需带 `[Required]`/`[StringLength]` 等校验特性
- 更新时先 `FindAsync` 校验存在，再 `_mapper.Map(form, exists)` 合并到已跟踪实体，最后 `UpdateAsync`
- 删除、启停切换等操作同样返回 BaseMessage

### 3. AutoMapper：每个实体一个独立 Profile

- Profile 放在 `Pruner.Host/Profiles`，**一个实体对应一个 Profile 文件**（禁止合并写多个映射）
- 命名：`DsXxxProfile.cs`（如 `DsDeleteConfigProfile`），内部只含该实体的映射
- Startup 已通过 `AddAutoMapper(Assembly.Load("Pruner.Host"))` 自动扫描注册，无需手工注册

```csharp
public class DsDeleteConfigProfile : Profile
{
    public DsDeleteConfigProfile()
    {
        CreateMap<DsDeleteConfigForm, DsDeleteConfig>();
    }
}
```

### 4. 通用约定

- 所有管理接口 `[AllowAnonymous]`（无需登录），路由 `[Route("api/[controller]")]`
- 控制器基类统一使用 `Controller`（非 `ControllerBase`），并加 `[ApiController]`
- 响应成功使用**静态调用** `BaseMessage.Success("消息", data)`（双参数必须静态形式，实例调用会报 CS0176）；失败用实例 `msg.Fail(...)`
- 错误类型使用 `BaseErrType` 枚举（`InvalidParameter`/`DataNotFound`/`ServerError` 等）

## 三、数据访问规范（Repository）

### 1. 查询条件表达式构建下沉至仓储层

- **Controller 禁止构建 where 表达式**，只通过简单参数（字符串、数值、时间等）调用仓储方法
- 表达式构建、过滤、排序、分页逻辑全部在仓储实现内完成

```csharp
// 接口（Domain 层）
Task<PageList<DsDeleteConfig>> GetPageListAsync(int pageIndex, int pageSize, string tableName = "");

// 实现（Repository 层）
public async Task<PageList<DsDeleteConfig>> GetPageListAsync(int pageIndex, int pageSize, string tableName = "")
{
    var query = DbSet.AsQueryable();
    if (!string.IsNullOrWhiteSpace(tableName))
        query = query.Where(e => e.TableName.Contains(tableName));

    var total = await query.CountAsync();
    var list = await query
        .OrderByDescending(e => e.Id)
        .Skip((pageIndex - 1) * pageSize)
        .Take(pageSize)
        .ToListAsync();

    return new PageList<DsDeleteConfig>(total, pageIndex, pageSize, list);
}
```

- 分页必须返回总数（`CountAsync`），因为框架 `PageList` 的构造会自动计算 `TotalPage`
- 仓储继承 `OneForAll.EFCore.Repository<T>`，使用 `DbSet` 直接 LINQ 查询

## 四、前端规范（wwwroot/index.html）

- 单页应用：Vue 3 + Element Plus（本地 lib/ 目录托管，不依赖外网 CDN，新增需从 npmmirror 下载对应版本文件）
- **根 DOM 模板（#app 内的 HTML）中组件标签必须显式闭合**（`<el-input></el-input>`），禁止自闭合写法（`<el-input />`）：浏览器 HTML 解析器不识别自定义组件自闭合，后续内容会被嵌套进前一个组件（典型症状：el-table 只渲染一列/列错位/分组表头）；字符串模板（`template: '...'`）由 Vue 编译器解析，不受此限制
- **数据字段一律大驼峰**，与后端实体属性一致（后端 `DefaultContractResolver` 原样序列化）：
  - 查询响应直接取 `res.Items` / `res.Total`（PageList）或数组本身（列表接口）
  - 表格 `prop`、`row.Id`、`globalForm.IsEnabled` 等全部大驼峰
- 写操作响应为 BaseMessage：成功判断 `res.Status === true`，提示取 `res.Message`
- `request` 封装统一处理错误：HTTP 失败或响应 `Status === false` 时抛出异常，调用处 catch 提示 `e.message`
- API 使用相对路径 `/api/...`

## 五、其他约定

- JSON 序列化：保留属性原大小写（`DefaultContractResolver`），日期格式 `yyyy-MM-dd HH:mm:ss`
- 运行日志实体 `DsRunningLog` 的 `TypeName` 固定为任务类型名（如 `MonitorDbDeleteJob`）
- 定时任务配置在 `appsettings.json` 的 `Quartz` 节点，任务类放 `Pruner.Host/QuartzJobs`
